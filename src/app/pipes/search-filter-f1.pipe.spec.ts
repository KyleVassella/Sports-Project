import { SearchFilterF1Pipe } from './search-filter-f1.pipe';

describe('SearchFilterF1Pipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilterF1Pipe();
    expect(pipe).toBeTruthy();
  });
});
