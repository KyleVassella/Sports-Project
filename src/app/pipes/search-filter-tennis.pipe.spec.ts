import { SearchFilterTennisPipe } from './search-filter-tennis.pipe';

describe('SearchFilterTennisPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilterTennisPipe();
    expect(pipe).toBeTruthy();
  });
});
